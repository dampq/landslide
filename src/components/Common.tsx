import {Card,Badge,Button} from "./ui";import {AlertTriangle,ArrowUpRight,Activity} from "lucide-react";import RiskBadge from "./RiskBadge";import {Alert} from "../types";
export function PageHead({title,sub,action}:{title:string,sub:string,action?:React.ReactNode}){return <div className="mb-5 flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-2xl font-black text-slate-900 md:text-3xl">{title}</h1><p className="mt-1 text-sm text-slate-500">{sub}</p></div>{action}</div>}
export function Metric({label,value,trend,tone="blue"}:{label:string,value:string|number,trend?:string,tone?:string}){const c:any={red:"bg-red-50 text-red-700",orange:"bg-orange-50 text-orange-700",amber:"bg-amber-50 text-amber-700",green:"bg-emerald-50 text-emerald-700",blue:"bg-blue-50 text-blue-700"};return <Card className="p-4"><div className="flex items-center justify-between"><div className={`rounded-xl p-2 ${c[tone]||c.blue}`}><Activity size={18}/></div>{trend&&<span className="text-xs font-semibold text-slate-500">{trend}</span>}</div><div className="mt-4 text-3xl font-black text-slate-900">{value}</div><div className="text-sm font-semibold text-slate-500">{label}</div></Card>}
export function AlertCard({a,onAck,onResolve}:{a:Alert,onAck?:()=>void,onResolve?:()=>void}){return <Card className={`p-5 ${a.severity==="Critical"?"border-l-4 border-l-red-600":a.severity==="High"?"border-l-4 border-l-orange-500":""}`}><div className="flex flex-wrap justify-between gap-2"><div><div className="flex items-center gap-2"><AlertTriangle size={19} className={a.severity==="Critical"?"text-red-600":"text-orange-500"}/><b>{a.type}</b><RiskBadge level={a.severity}/></div><p className="mt-1 text-sm font-semibold text-slate-600">{a.location} · {a.time}</p></div><span className="text-sm text-slate-500">{a.people.toLocaleString()} people potentially affected</span></div><p className="mt-4 text-sm text-slate-700">{a.description}</p><div className="mt-3 rounded-xl bg-slate-50 p-3 text-sm"><b>Recommended action:</b> {a.action}</div><div className="mt-4 flex flex-wrap gap-2"><Button variant="outline" onClick={onAck}>{a.ack?"Acknowledged":"Acknowledge"}</Button><Button variant="outline">View Location</Button><Button>Notify Authorities</Button><Button variant="danger">Send Public Warning</Button>{onResolve&&<Button variant="ghost" onClick={onResolve}>Mark Resolved</Button>}</div></Card>}
