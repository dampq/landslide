import {useState} from "react";import {PageHead,AlertCard} from "../components/Common";import {alerts as seed} from "../data/mock";import {Button} from "../components/ui";
export default function Alerts(){const [a,setA]=useState(seed);return <><PageHead title="Real-Time Alert Centre" sub="Acknowledge, notify and resolve emergency intelligence" action={<Button variant="danger">Issue New Public Warning</Button>}/><div className="space-y-4">{a.filter(x=>!x.resolved).map(x=><AlertCard a={x} onAck={()=>setA(v=>v.map(y=>y.id===x.id?{...y,ack:true}:y))} onResolve={()=>setA(v=>v.map(y=>y.id===x.id?{...y,resolved:true}:y))}/>)}</div></>}