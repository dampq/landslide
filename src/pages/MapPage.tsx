import {useState} from "react";import {PageHead} from "../components/Common";import {Card,Badge} from "../components/ui";import RiskMap from "../components/RiskMap";
export default function MapPage(){const layers=["Landslide Risk","Rainfall","Soil Moisture","Road Network","Villages","Hospitals","Emergency Centres","Sensors","Historical Landslides"];const [on,setOn]=useState(layers);return <><PageHead title="Live GIS Risk Map" sub="Interactive geospatial intelligence across North Eastern India"/><div className="relative"><Card className="overflow-hidden"><RiskMap/></Card><Card className="absolute left-3 top-3 z-[500] max-h-[70%] w-52 overflow-auto p-3"><b className="text-sm">Map Layers</b>{layers.map(x=><label className="mt-2 flex items-center gap-2 text-xs" key={x}><input type="checkbox" checked={on.includes(x)} onChange={()=>setOn(v=>v.includes(x)?v.filter(y=>y!==x):[...v,x])}/>{x}</label>)}</Card><Card className="absolute bottom-5 right-3 z-[500] p-3"><b className="text-xs">Risk Legend</b><div className="mt-2 flex flex-wrap gap-2">{[["Low","bg-emerald-500"],["Moderate","bg-yellow-500"],["High","bg-orange-500"],["Critical","bg-red-600"]].map(([x,c])=><Badge className={`${c} text-white`}>{x}</Badge>)}</div></Card></div></>}